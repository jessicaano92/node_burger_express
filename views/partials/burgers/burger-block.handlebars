
				<!--refactored-->

<li>   <!--populates the burger names and buttons on the page-->
	{{burger_name}}

{{#if devoured}}
<button class="delete-burger"  data-id="{{this.id}}" >Delete!!</button>
{{else}}
<button class="change-devoured" data-id="{{this.id}}" data-newdevoured="{{devoured}}"> DEVOUR!!</button>
{{/if}}
</li>